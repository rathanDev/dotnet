meta {
  name: Login via ApiGw
  type: http
  seq: 3
}

post {
  url: http://localhost:5000/users/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "admin",
    "password": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
